{
  "name": "ecosort-web-admin",
  "private": true,
  "type": "module",
  "description": "Supabase client (JS) cho web quản trị — tách khỏi app Flutter",
  "dependencies": {
    "@supabase/supabase-js": "^2.101.1"
  },
  "devDependencies": {
    "autoprefixer": "^10.5.0",
    "esbuild": "^0.28.0",
    "postcss": "^8.5.26",
    "tailwindcss": "^3.4.1",
    "vite": "^8.2.2"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "overrides": {
    "esbuild": "$esbuild",
    "postcss": "$postcss",
    "vite": "$vite"
  }
}
